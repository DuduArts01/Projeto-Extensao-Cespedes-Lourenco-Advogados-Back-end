ktor {
    deployment {
        host = "0.0.0.0"  # CRÍTICO: deve ser 0.0.0.0, NÃO localhost
        port = 8080
        port = ${?PORT}   # Lê a variável PORT do ambiente
    }
    application {
        modules = [ com.cespedes.ApplicationKt.module ]  # Ajuste para seu pacote
    }
}